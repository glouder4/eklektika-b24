# R25 — задачи: портальная головная → дочерние (скидка, UF участников)

## Выполнено

- [x] R25-01 — `CompanySync::onAfterCompanyUpdate`: ensure ИБ 34, пересчёт `holding_group_members` и проброс `discount` для `company.head_company_flag`.
- [x] R25-03 — Пересчёт `UF_CRM_1776426878` по кластеру при сохранении дочерней (`holdingMembersSyncOnChildSave`).
- [x] R25-06 — Резолв элемента ИБ 34 для кластера при портальной головной без `company.is_head`: `resolveHoldingIblockElementForOutboundCluster` + `resolveHoldingCompanyB24Ids` учитывают `head_company_flag`.

## Осталось

- [ ] R25-02 — Ручной смоук: головная с `UF_CRM_1758028888`, две дочерние с `UF_CRM_1758028816` = ID элемента ИБ 34; изменить скидку у головной (без смены UF в `$arFields` вручную) → у дочерних совпадает `UF_CRM_1771490556028`; у всех совпадает состав `UF_CRM_1776426878`.
- [ ] R25-04 — Ручной смоук: изменить у дочерней только поле без UF холдинга в теле запроса (например название) → у головной и второй дочерней обновился `UF_CRM_1776426878` (тот же набор ID).

## Бэклог (не блокер)

- [ ] R25-05 — По желанию: вынести чистую CRM-логику кластера холдинга в сервис в `yomerch.b24.contract` и оставить в `CompanySync` только вызовы + outbound; оценить порядок событий и тесты.
